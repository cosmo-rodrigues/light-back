// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum State {
  AC
  AL
  AP
  AM
  BA
  CE
  DF
  ES
  GO
  MA
  MT
  MS
  MG
  PA
  PB
  PR
  PE
  PI
  RJ
  RN
  RS
  RO
  RR
  SC
  SP
  SE
  TO
}

model Cliente {
  id              Int          @id @unique @default(autoincrement())
  nome            String
  numeroDoCliente String       @map("numero_do_cliente")
  cliente         Fatura[]
  endereco        Endereco[]
  instalacao      Instalacao[]

  @@map("clientes")
}

model Endereco {
  id          Int          @id @unique @default(autoincrement())
  endereco    String
  numero      String?
  semNumero   Boolean      @default(true) @map("sem_numero")
  complemento String?
  bairro      String
  cidade      String
  estado      State
  cep         String?
  cliente     Cliente?     @relation(fields: [clienteId], references: [id])
  clienteId   Int?         @map("cliente_id")
  instalacao  Instalacao[]
  fatura      Fatura[]

  @@map("enderecos")
}

model Instalacao {
  id                 Int       @id @unique @default(autoincrement())
  numeroDaInstalacao String    @map("numero_da_instalacao")
  cliente            Cliente   @relation(fields: [clienteId], references: [id])
  clienteId          Int       @map("cliente_id")
  endereco           Endereco? @relation(fields: [enderecoId], references: [id])
  enderecoId         Int?      @map("endereco_id")
  fatura             Fatura[]

  @@map("instalacoes")
}

model Fatura {
  id                              Int         @unique @default(autoincrement())
  chaveDeAcesso                   String      @unique @map("chave_de_acesso")
  dataDoVencimento                String      @map("data_do_vencimento")
  dataEmissao                     String      @map("data_emissao")
  datasDeLeituraAnterior          String      @map("datas_de_leitura_anterior")
  datasDeLeituraAtual             String      @map("datas_de_leitura_atual")
  datasDeLeituraProxima           String      @map("datas_de_leitura_proxima")
  diasUtilizando                  String      @map("dias_utilizando")
  energiaCompensadaquantidade     String      @map("energia_compensada_quantidade")
  energiaCompensadaprecoUnitario  String      @map("energia_compensada_preco_unitario")
  energiaCompensadatarifaUnitaria String      @map("energia_compensada_tarifa_unitaria")
  energiaCompensadavalorTotal     String      @map("energia_compensada_valor_total")
  energiaEletricaquantidade       String      @map("energia_eletrica_quantidade")
  energiaEletricaprecoUnitario    String      @map("energia_eletrica_preco_unitario")
  energiaEletricatarifaUnitaria   String      @map("energia_eletrica_tarifa_unitaria")
  energiaEletricavalorTotal       String      @map("energia_eletrica_valor_total")
  energiaSCEEquantidade           String      @map("energia_scee_quantidade")
  energiaSCEEprecoUnitario        String      @map("energia_scee_preco_unitario")
  energiaSCEEtarifaUnitaria       String      @map("energia_scee_tarifa_unitaria")
  energiaSCEEvalorTotal           String      @map("energia_scee_valor_total")
  bairro                          String
  cep                             String
  cidade                          String
  complemento                     String?
  estado                          String
  numero                          String?
  rua                             String
  faturaReferenteA                String      @map("fatura_referente_a")
  linkParaConsulta                String      @map("link_para_consulta")
  nomeDoCliente                   String      @map("nome_do_cliente")
  numeroDaInstalacao              String      @map("numero_da_instalacao")
  numeroDaNotaNF                  String      @unique @map("numero_da_nota_nf")
  numeroDoCliente                 String      @map("numero_do_cliente")
  awsStorageObjectKey             String?     @map("aws_storage_object_key")
  valorTotalDaFatura              String      @map("valor_total_da_fatura")
  cliente                         Cliente?    @relation(fields: [clienteId], references: [id])
  clienteId                       Int?        @map("cliente_id")
  endereco                        Endereco?   @relation(fields: [enderecoId], references: [id])
  enderecoId                      Int?        @map("endereco_id")
  instalacao                      Instalacao? @relation(fields: [instalacaoId], references: [id])
  instalacaoId                    Int?        @map("instalacao_id")

  @@map("faturas")
}
